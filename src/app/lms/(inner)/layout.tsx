import React from "react";
const LmsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      <h1>LMS Layout</h1>
      {children}
    </div>
  );
};

export default LmsLayout;
